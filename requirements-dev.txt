# Local dev / CI tooling only — never installed into the Airflow image (see Dockerfile),
# so these can be pinned freely without colliding with Airflow's constraints file.
-r requirements.txt

pytest==8.2.2
ruff==0.5.0
black==24.4.2
pre-commit==3.7.1
