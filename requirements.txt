# Extra packages layered onto the official apache/airflow image (see docker-compose.yaml).
# apache-airflow itself is deliberately NOT pinned here — it already ships in the base
# image, and repinning it here would fight Airflow's own dependency constraints.
pandas==2.2.2
mysql-connector-python==8.4.0
psycopg2-binary==2.9.9
apache-airflow-providers-mysql==5.7.0
apache-airflow-providers-postgres==5.13.0
python-dotenv==1.0.1
matplotlib==3.9.0

# Dev/test/lint tooling — used locally (in a Python 3.11/3.12 venv, matching the
# Airflow image) and in CI; not required inside the running Airflow containers.
pytest==8.2.2
ruff==0.5.0
black==24.4.2
pre-commit==3.7.1
